package test;

public class New {
	public static int binarySearch(Integer[] srcArray, int des) {
	    //定义初始最小、最大索引
	    int low = 0;
	    int high = srcArray.length - 1;
	    //确保不会出现重复查找，越界
	    while (low <= high) {
	        //计算出中间索引值
	        int middle = (high + low)>>>1 ;//防止溢出
	        if (des == srcArray[middle]) {
	            return middle;
	        //判断下限
	        } else if (des < srcArray[middle]) {
	            high = middle - 1;
	        //判断上限
	        } else {
	            low = middle + 1;
	        }
	    }
	    //若没有，则返回-1
	    return -1;
	}
	public static Integer[] bubblesort(Integer[] array,int length){
		int max=0;
		int flag=0;
		for(int j=length-1;j>0;--j)
		{
			
			for (int i=0;i<=j-1;++i){
				if (array[i]>max){
				 max=array[i];
				 flag=i;
				}
			}
			replace(array,j,flag);
		}
		return array;
	}
	public static Integer[] bubble(Integer[] array,int length){
		for (int i=0;i<=length-1;i++){
			bubblesort(array,i);
		}
		return array;
	}
	public static void replace(Integer[] array,int length,int i){
		int temp=0;
		int last=length;
		temp=array[last];
		array[last]=array[i];
		array[i]=last;
	}
	public static void main(String[] args) {
	    Integer[] array = {4,5,62,3,1,0,38,7};
	    int length= array.length;
	    Integer[] array2=bubblesort(array,length);
	    for (int i=0;i<=length-1;i++){
	    	System.out.println(array2[i]);
		}
		//System.out.println(binarySearch(array,7));
	}
	
	public static Integer[] bubble(Integer[] arr){
		int count = 0;
		for (int i = 0; i < arr.length-count-1; i++) {
			for (int j = 0; j < arr.length-1; j++) {
				if(arr[j]>=arr[j+1]){
					int temp = arr[j];
					arr[j] = arr[j+1];
					arr[j+1] = temp;
				}
			}
			count++;
		}
		return arr;
	}
}
